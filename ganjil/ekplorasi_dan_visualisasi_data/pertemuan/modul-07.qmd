---
title: "Pertemuan 7 : Data Visualization (R)"
image: https://raw.githubusercontent.com/aslab-math-ui/asset/main/eksplorasi_dan_visualisasi_data/static/images/plotly.png
description: "Data Visualization using R (Part 1)"
date: 11/18/2024
page-navigation: true
format: html
engine: knitr
---

<a href="../eda2025.qmd" class="btn btn-outline-primary mb-4">
<i class="fa-solid fa-arrow-left"></i> Kembali ke Arsip Praktikum EVDA 2025
</a>

# Pendahuluan
## Mengambil Data
```{r}
#| message: false
#| warning: false

library(tidyverse)
library(palmerpenguins)
library(ggthemes)
penguins
glimpse(penguins)
```

## Membuat "Canvas" 
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
)
```

## Membuat sumbu-sumbu (axis)
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
)
```

## Menambahkan titik-titik
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
) +
  geom_point()
```

## Menambahkan Warna 
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g, color = species)
) +
  geom_point()
```

## Menambahkan garis regresi
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g, color = species)
) +
  geom_point() +
  geom_smooth(method = "lm")
```

## Menambahkan garis regresi global
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
) +
  geom_point(mapping = aes(color = species)) +
  geom_smooth(method = "lm")
```

## Menambahkan variasi titik
```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
) +
  geom_point(mapping = aes(color = species, shape = species)) +
  geom_smooth(method = "lm")
```

## Mengganti color palette

```{r}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
) +
  geom_point(aes(color = species, shape = species)) +
  geom_smooth(method = "lm") +
  labs(
    title = "Body mass and flipper length",
    subtitle = "Dimensions for Adelie, Chinstrap, and Gentoo Penguins",
    x = "Flipper length (mm)", y = "Body mass (g)",
    color = "Species", shape = "Species"
  ) +
  scale_color_colorblind()
```


# Mem-visualisasikan Distribusi Variabel Kategorik
```{r}
ggplot(penguins, aes(x = species)) +
  geom_bar()
```

## Mengatur sumbu
```{r}
ggplot(penguins, aes(x = fct_infreq(species))) +
  geom_bar()
```
